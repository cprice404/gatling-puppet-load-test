- job-template:
    name: '{task}-{name}-010-provision-sut'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be provisioning your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-020-install-pe'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-020-install-pe'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be installing PE on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-030-customize-settings'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-030-customize-settings'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be customizing settings on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-040-install-puppet-code'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-040-install-puppet-code'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be installing Puppet Code into the staging directory on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-050-file-sync'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-050-file-sync'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be performing a file sync on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-060-classify-nodes'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-060-classify-nodes'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be classifying nodes on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-070-validate-classification'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-070-validate-classification'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be classifying nodes on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-080-launch-bg-scripts'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-080-launch-bg-scripts'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be launching background scripts on your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-090-run-gatling-sim'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-090-run-gatling-sim'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be running a gatling simulation against your SUT, but I'm not!"

    publishers:
      - trigger-parameterized-builds:
        - project: '{task}-{name}-100-collect-artifacts'
          trigger-with-no-params: true

- job-template:
    name: '{task}-{name}-100-collect-artifacts'

    builders:
      - shell: |
          #!/usr/bin/env bash

          echo "Hi!  I should be collecting reports / artifacts for your perf test, but I'm not!"

- job-group:
    name: "{task}-{name}-pipeline"
    jobs:
      - "{task}-{name}-010-provision-sut"
      - "{task}-{name}-020-install-pe"
      - "{task}-{name}-030-customize-settings"
      - "{task}-{name}-040-install-puppet-code"
      - "{task}-{name}-050-file-sync"
      - "{task}-{name}-060-classify-nodes"
      - "{task}-{name}-070-validate-classification"
      - "{task}-{name}-080-launch-bg-scripts"
      - "{task}-{name}-090-run-gatling-sim"
      - "{task}-{name}-100-collect-artifacts"
